// o(1)
// For this case, it doesn't matter the value of n, the number of operations will always be 1.
// This notation is usually referred to as CONSTANT TIME. It is the most efficient Big-O notation

function addItems(n) {
    return n + n;
}
